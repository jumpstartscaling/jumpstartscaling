{
  "name": "jumpstart-multisite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview --host",
    "deploy": "npm run build && ./deploy.sh",
    "verify": "bash scripts/verify-install.sh",
    "configure-coolify": "node scripts/configure-coolify-via-api.mjs"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
