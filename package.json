{
    "name": "jumpstart-multisite",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "astro dev --host",
        "build": "astro build",
        "preview": "astro preview --host",
        "deploy": "npm run build && ./deploy.sh"
    },
    "dependencies": {
        "pg": "^8.11.3",
        "dotenv": "^16.3.1"
    },
    "devDependencies": {
        "@types/node": "^22.10.5",
        "typescript": "^5.7.3"
    }
}