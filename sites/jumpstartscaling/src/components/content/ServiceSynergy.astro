---
const services = [
  {
    id: 'funnel',
    title: 'Funnel Architecture',
    desc: 'Paid ads are useless if your landing page leaks. We fix the bucket before turning on the hose.',
    link: '/services/funnel-architecture',
    icon: '⚡'
  },
  {
    id: 'crm',
    title: 'CRM Transformation',
    desc: 'Leads need to be nurtured. We automate the follow-up so no lead is left behind.',
    link: '/services/crm-transformation',
    icon: '🔄'
  },
  {
    id: 'data',
    title: 'Data Attribution',
    desc: 'Stop guessing which ads work. We implement server-side tracking to see the truth.',
    link: '/services/data-attribution',
    icon: '📊'
  }
];
---

<section class="synergy-section">
  <div class="container">
    <div class="header">
      <span class="mono-label">FULL STACK GROWTH</span>
      <h2>How It Works <span class="accent-text">Together</span></h2>
      <p>The Paid Acquisition Engine is just one combustion chamber. To get maximum velocity, connect it with our other systems.</p>
    </div>

    <div class="synergy-grid">
      {services.map(service => (
        <a href={service.link} class="synergy-card">
          <div class="icon">{service.icon}</div>
          <h3>{service.title}</h3>
          <p>{service.desc}</p>
          <div class="learn-more">Explore System →</div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .synergy-section {
    padding: 6rem 0;
    background: #050505;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .mono-label {
    font-family: 'Courier New', monospace;
    color: #888;
    letter-spacing: 2px;
    font-size: 0.9rem;
    display: block;
    margin-bottom: 1rem;
  }

  h2 {
    font-size: 2.5rem;
    color: #fff;
    margin-bottom: 3rem;
    font-weight: 800;
  }

  .accent-text {
    color: #fff;
    border-bottom: 2px solid #C9A961;
  }

  .synergy-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .synergy-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .synergy-card:hover {
    transform: translateY(-5px);
    border-color: #C9A961;
    background: rgba(201, 169, 97, 0.05);
  }

  .icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  h3 {
    color: #fff;
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
    font-weight: 700;
  }

  p {
    color: #aaa;
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    flex-grow: 1;
  }

  .learn-more {
    color: #C9A961;
    font-weight: 600;
    font-size: 0.9rem;
  }
</style>
